<!--div>
    <h3>{{!caption}}</h3>
    {{!#each top5}}
        <p>{{!shortNameInitialLast}}</p>
    {{!/each}}
</div-->

<div class="dashboard-top5-panel">
<h4>{{caption}}</h4>
<table class="table top5">
    <thead>
    <tr>
        <th class="th1">Player</th>
        <th class="th2">Gr / Gm</th>
    </tr>
    </thead>
    <tbody>
    {{#each player in filteredContent}}
        {{#link-to "player" player tagName="tr"}}
            <td class="td1">{{#link-to "player" player}}{{player.shortNameInitialLast}} / {{player.position}}{{/link-to}}</td>
            <td class="td2">{{player.seasonAvgGrade}}% / {{player.seasonNumGames}}</td>
        {{/link-to}}
    {{else}}
        <tr>
            <td>no players...</td>
        </tr>
    {{/each}}
    </tbody>
</table>
</div>
