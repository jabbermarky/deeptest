import DS from 'ember-data';

// a fixture adapter is a way to put hardcoded sample data into the app and not need a server
// put the hardcoded data into the model
export default DS.FixtureAdapter.extend({});

// a RESTful adapter looks like this
//export default DS.RESTAdapter.extend({
//});
